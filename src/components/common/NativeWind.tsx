import { LinearGradient } from 'expo-linear-gradient';
import { styled } from 'nativewind';
import { Text, View, TextInput, TouchableOpacity, SafeAreaView, Image, ScrollView } from 'react-native';

export const UIView = styled(View);
export const UIText = styled(Text);
export const UISafeAreaView = styled(SafeAreaView);
export const UILinearGradient = styled(LinearGradient);
export const UITextInput = styled(TextInput);
export const UITouchableOpacity = styled(TouchableOpacity);
export const UIImage = styled(Image);
export const UIScrollView = styled(ScrollView);
