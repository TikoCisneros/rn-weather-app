import * as Progress from 'react-native-progress';

// TODO: Add progress variations according react-native-progress
const ProgressBar = (props: Progress.CircleSnailPropTypes) => {
  return <Progress.CircleSnail {...props} />;
};

export default ProgressBar;
